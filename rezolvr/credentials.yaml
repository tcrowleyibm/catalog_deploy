name: dbcredentials
type: resource.environment
description: Credentials for the database
provides:
  - type: environment.secret
    name: databaseCredentials
    description: Secrets for accessing a database
    params:
      - name: db_username
        value: notdefined
      - name: db_password
        value: notdefined
